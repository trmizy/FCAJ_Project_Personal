# Architecture Diagram Source

This folder must contain the editable draw.io source file:

- `fitness-assistant-aws-architecture.drawio`

A minimal placeholder `.drawio` file is included so the download link in the report does not 404. It must be replaced with a real diagram (matching the topology described in Proposal Section 12 and Workshop 5.2) before this report is submitted.
