# Architecture Diagram

This folder must contain the final exported architecture diagram:

- `fitness-assistant-aws-architecture.png` — exported PNG of the real architecture diagram, referenced from `content/2-Proposal/` and `content/5-Workshop/5.2-Architecture/`.

The corresponding editable source (`fitness-assistant-aws-architecture.drawio`) belongs in `static/files/architecture/`.

Do not create a fake or generic AWS architecture image. The diagram must reflect what is actually described in the Proposal and Workshop 5.2 (application gateway container vs. Amazon API Gateway distinction, MVP services only, RDS in a private subnet, etc.). Until the real diagram exists, this file is a placeholder and any page referencing it should be treated as showing a broken/placeholder image.
