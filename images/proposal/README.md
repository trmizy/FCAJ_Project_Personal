# Proposal Images

Optional supporting images for the Proposal page (e.g. a whiteboard photo of the initial architecture sketch, if one exists). Not required if the main architecture diagram in `images/workshop/architecture/` already covers this.
