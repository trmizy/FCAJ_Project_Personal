# S3 Screenshots

This folder is currently empty because Amazon S3 is **Planned**, not **Implemented** (see Workshop 5.10). Only add screenshots here once a real S3 integration has been coded and tested in `user-service` and actually deployed — do not add screenshots of an unrelated/generic S3 bucket as if it were part of this application.
