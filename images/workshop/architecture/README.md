# Architecture Diagram

This folder must contain the final exported architecture diagram:

- `fitness-assistant-aws-architecture.png` — exported PNG of the real architecture diagram, referenced from `content/2-Proposal/` and `content/5-Workshop/5.2-Architecture/`.

The corresponding editable source (`fitness-assistant-aws-architecture.drawio`) belongs in `static/files/architecture/`.

Do not create a fake or generic AWS architecture image. The diagram must reflect what is actually described in the Proposal and Workshop 5.2 (application gateway container vs. Amazon API Gateway distinction, MVP services only, RDS in a private subnet, etc.).

**Status:** the editable source (`static/files/architecture/fitness-assistant-aws-architecture.drawio`) is now a real diagram matching the current MVP scope — see that folder's `README.md` for what it contains and the verification steps. This `.png` is the **one remaining manual step**: it cannot be generated by a script here — export it from the `.drawio` file (draw.io desktop app or [app.diagrams.net](https://app.diagrams.net/), File → Export as → PNG) and replace this placeholder before submission. Until that export is done, this file is still a placeholder and any page referencing it is showing a placeholder image, not the real diagram.
