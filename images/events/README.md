# Event Photos

One folder or filename set per event attended, e.g. `event-1-01.png`, `event-1-02.png`. Only add real photos/screenshots after actually attending an event — do not add placeholder or stock images here.
